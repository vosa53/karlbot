/**
 * A symbol identified during a semantic analysis.
 */
export abstract class Symbol_ { // "_" suffix is here because webpack (in contrast to Angular compiler) can not solve name clash with JavaScript native Symbol class.
    
}