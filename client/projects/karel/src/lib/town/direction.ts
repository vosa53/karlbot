/**
 * Direction in a town.
 */
export enum Direction {
    /**
     * Up (north).
     */
    up = 0,

    /**
     * Right (east).
     */
    right = 1,

    /**
     * Down (south).
     */
    down = 2,

    /**
     * Left (west).
     */
    left = 3
}
