/**
 * Direction in a town.
 */
export enum Direction {
    /**
     * Up (north).
     */
    up = 0,

    /**
     * Left (west).
     */
    left = 1,

    /**
     * Down (south).
     */
    down = 2,

    /**
     * Right (east).
     */
    right = 3
}